#include <stdio.h>
#include <stdbool.h>
#include <string.h>
#include <math.h>

int main(){
    
    double A = sqrt(9);
    double B = pow (2,4);
    double C = ceil(3.14);
    double D = floor(3.99);
    double E = fabs(-10);
    double F = log(3);
    double G = sin(45);

    printf("\n%lf", A);
    printf("\n%lf", B);
    printf("\n%lf", C);
    printf("\n%lf", D);

    return 0;
}